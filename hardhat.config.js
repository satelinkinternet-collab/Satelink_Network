import "@nomicfoundation/hardhat-ethers";
import "dotenv/config";

console.log("Loading hardhat.config.js (ESM)");

// Default config will spin up a local Hardhat network suitable for testing
export default {
  solidity: "0.8.28",
};
