{
  "name": "satelink-mvp",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@11.6.2",
  "type": "module",
  "workspaces": [
    "apps/*",
    "services/*",
    "packages/*",
    "agents/*"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "turbo": "^2.1.2",
    "typescript": "~5.8.0"
  },
  "scripts": {
    "install:all": "npm install",
    "dev": "concurrently \"npm run dev --workspace=@satelink/api\" \"npm run dev --workspace=web\"",
    "dev:api": "npm run dev --workspace=@satelink/api",
    "dev:dashboard": "npm run dev --workspace=web",
    "start": "npm run start --workspace=@satelink/api",
    "gateway": "npm run gateway --workspace=@satelink/api",
    "worker": "npm run worker --workspace=@satelink/api",
    "build": "turbo build",
    "test": "turbo test",
    "lint": "turbo lint",
    "contracts:build": "forge build",
    "contracts:test": "forge test -vv",
    "contracts:deploy:local": "forge script script/Deploy.s.sol --rpc-url localhost --broadcast",
    "contracts:deploy:spark": "forge script script/Deploy.s.sol --rpc-url fuse_spark --broadcast"
  },
  "dependencies": {
    "dotenv": "^17.4.1",
    "ethers": "^6.16.0"
  }
}
