<%- include('partials/header', { activePage: 'builder_docs' }) %>

    <div class="max-w-4xl mx-auto">
        <div class="mb-8">
            <a href="/ui/builder"
                class="text-satelink-muted hover:text-gray-900 text-sm mb-4 inline-flex items-center transition-colors">
                <%- include('partials/icon', { name: 'arrow-right-from-bracket' , className: 'w-3 h-3 rotate-180 mr-2'
                    }) %> Back to Dashboard
            </a>
            <h1 class="text-3xl font-bold text-gray-900 mb-2">Integration Guide</h1>
            <p class="text-satelink-muted text-lg">How to use the Satelink Builder API to monetize your services.</p>
        </div>

        <div class="space-y-8">
            <!-- Overview -->
            <div class="bg-white p-8 rounded-xl border border-satelink-border shadow-sm">
                <h2 class="text-xl font-bold text-gray-900 mb-4">Overview</h2>
                <p class="text-gray-600 mb-4 leading-relaxed">
                    The Builder API allows you to report usage and charge users for services you provide.
                    Satelink handles the settlement, debiting the user's wallet and crediting your builder account.
                </p>
            </div>

            <!-- Authentication -->
            <div class="bg-white p-8 rounded-xl border border-satelink-border shadow-sm">
                <h2 class="text-xl font-bold text-gray-900 mb-4">Authentication</h2>
                <p class="text-gray-600 mb-4">
                    All requests must include your API Key in the <code
                        class="bg-gray-100 px-2 py-0.5 rounded text-satelink-accent font-bold border border-gray-200">x-api-key</code>
                    header.
                </p>
            </div>

            <!-- Reporting Usage -->
            <div class="bg-white p-8 rounded-xl border border-satelink-border shadow-sm">
                <h2 class="text-xl font-bold text-gray-900 mb-4">1. Report Usage</h2>
                <p class="text-gray-600 mb-4">
                    Call this endpoint whenever a user consumes a billable unit of your service.
                </p>

                <div
                    class="bg-slate-900 rounded-lg border border-slate-800 p-4 mb-4 font-mono text-sm overflow-x-auto text-gray-300">
                    <span class="text-purple-400 font-bold">POST</span> <span
                        class="text-white">https://satelink-api.com/v1/builder/usage</span>
                </div>

                <h3 class="font-bold text-gray-900 mb-2 text-sm uppercase tracking-wider">Payload</h3>
                <pre
                    class="bg-slate-900 p-4 rounded-lg border border-slate-800 text-emerald-400 text-xs mb-6 overflow-x-auto shadow-inner">{
  "endpoint": "validate_address",    // Identify the service
  "qty": 1,                          // Quantity consumed
  "unit_price_usdt": 0.05,           // Price per unit
  "payer_wallet": "0x123...",        // User wallet to charge
  "meta": { "req_id": "abc-123" }    // Optional metadata
}</pre>

                <h3 class="font-bold text-gray-900 mb-2 text-sm uppercase tracking-wider">Example Request</h3>
                <pre
                    class="bg-slate-900 p-4 rounded-lg border border-slate-800 text-slate-300 text-xs overflow-x-auto shadow-inner">curl -X POST https://satelink-api.com/v1/builder/usage \
  <span class="text-purple-400">-H</span> <span class="text-yellow-300">"x-api-key: sl_live_1234..."</span> \
  <span class="text-purple-400">-H</span> <span class="text-yellow-300">"Content-Type: application/json"</span> \
  <span class="text-purple-400">-d</span> '{
    "endpoint": "api/verify",
    "qty": 1,
    "unit_price_usdt": 0.01,
    "payer_wallet": "0xUserWallet..."
  }'</pre>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>