<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/app.css">
    <script src="/js/dashboard.js" defer></script>
</head>

class="flex items-center space-x-3 px-4 py-3 rounded-lg hover:bg-slate-700/50 hover:text-white transition-colors <%=
    typeof activePage !=='undefined' && activePage==='logs' ? 'bg-satelink-accent/10 text-satelink-accent'
    : 'text-slate-300' %>">
    <i class="fas fa-terminal w-5 text-center"></i>
    <span>Logs</span>
    </a>
    </nav>

    <div class="p-4 border-t border-slate-700">
        <a href="/ui/logout"
            class="flex items-center space-x-3 px-4 py-2 text-sm text-red-400 hover:text-red-300 transition-colors">
            <i class="fas fa-sign-out-alt"></i>
            <span>Sign Out</span>
        </a>
    </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 flex flex-col overflow-hidden relative">
        <!-- Mobile Header -->
        <header class="md:hidden flex items-center justify-between p-4 bg-satelink-surface border-b border-slate-700">
            <span class="font-bold text-lg">SATELINK</span>
            <button class="text-slate-300"><i class="fas fa-bars"></i></button>
        </header>

        <div class="flex-1 overflow-y-auto p-6 md:p-8">